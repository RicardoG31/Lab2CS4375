README

Find directory of command
	type /usr/bin/which <command>
	Ex. $ /usr/bin/which ls
Use command funciton 
	type /bin/<command>
	Ex. $ /bin/ls
Use instance of bash within shell
	type /bin/shell
	Inside of bash type "exit" to go back to shell 
Exit shell
	hold ctrl+d
